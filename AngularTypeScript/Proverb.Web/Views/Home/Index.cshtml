@section scripts {
    <script>@* Server config to inject into client code *@
        window.appConfig = {
            inDebug: @(AppConfigHelper.InDebug.ToString().ToLower()),
            remoteServiceRoot: "@Url.Content("~/api/")",
            version: "@(AppConfigHelper.Version)"
        };
    </script>
    @Scripts.Render("~/angularApp")
}

<div>
    <div ng-include="'/app/layout/shell.html?v=@(AppConfigHelper.Version)'"></div>
    <div id="splash-page" ng-show="false" class="dissolve-animation">
        <div class="page-splash">
            <div class="page-splash-message">
                Proverb
            </div>

            <div class="progress">
                <div class="progress-bar progress-bar-striped active" role="progressbar" style="width: 20%;">
                    <span class="sr-only">loading...</span>
                </div>
            </div>
        </div>
    </div>
</div>